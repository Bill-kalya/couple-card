# TODO — Pink & Sky-Blue Love Palette Refactor

- [x] Define a centralized love-themed color system (CSS variables) in `src/index.css` (pink/sky, warm neutrals, deep plum text, golden warmth).
- [x] Replace hard-coded color values in `src/App.module.css`, `src/CategoryPicker.module.css`, `src/Card3D.module.css`, and any other CSS files with the new variables.
- [x] Update dynamic/in-JS color fallbacks (e.g. ambient glow default) to use new variables (or hardcoded love colors matching variables).
- [x] Update category theme colors in `src/cards.json` to match the requested pink/sky + warmth bridge palette.
- [x] Run the build (and/or dev lint) to ensure no CSS/JS errors.



